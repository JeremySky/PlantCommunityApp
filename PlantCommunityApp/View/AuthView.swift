import SwiftUI

struct AuthView: View {
    var body: some View {
        VStack {
            Text("Image")
            Text("growflow")
            Text("a community for plant lovers")
            Button("Sign Up", action: {})
            Button("Log In", action: {})
        }
    }
}

#Preview {
    AuthView()
}
